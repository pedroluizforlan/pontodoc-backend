package com.pedroluizforlan.pontodoc.repository;

import com.pedroluizforlan.pontodoc.model.SupportTicketConversation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportTicketConversationRepository extends JpaRepository<SupportTicketConversation, Long> {
}
